@model BESTINVER.GestorAltas.Web.Management.Models.DocumentGroupData

@{
    Model.DisableUpdateSendingWays = true;
}

<div id='generateDocumentationModal' class='modal fade' role="dialog" aria-labelledby="generateDocumentationLabel">
    @using (Html.BeginForm("GenerateJuridicDocumentation", "Details", FormMethod.Post, new { @class = "container" }))
    {
        @await Html.PartialAsync("_GenerateDocPartialView", Model)
    }
</div>